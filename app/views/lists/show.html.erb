<p> <%= link_to "← back to collections", lists_path %> </p>

<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url('<%= @list.image_url %>');">
  <%# <p class="link-banner"><%= link_to "⇦ Back to colletions", lists_path %>
  <div class="container">
    <h1> <%= @list.name %> List </h1>
    <div class="btn-center">
      <p class="link-btn-light"><%= link_to "Add bookmark", new_list_bookmark_path(@list) %></p>
    </div>
  </div>
</div>

<div class="container">
  <div class="cards">
    <% @bookmarks.each do |bookmark| %>
      <div class="card-movie">
        <%= image_tag bookmark.movie.poster_url %>
        <div class="card-movie-info">
          <div>
            <div class="header">
              <h2> <%= bookmark.movie.title %></h2>
              <h2> <%= bookmark.movie.rating %> ⭐</h2>
            </div>
            <p><%= bookmark.movie.overview %></p>
          </div>
          <div>
            <p class="comment"> 💬 <%= bookmark.comment %></p>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
