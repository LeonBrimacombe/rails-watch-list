<div class="container mt-5">
  <h3> <%= @list.name %> New list </h3>

  <%= simple_form_for [@list] do |f| %>
    <%= f.input :name %>
    <%= f.input :image_url %>
    <%= f.submit %>
  <% end %>
</div>
